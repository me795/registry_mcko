<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.mcko.registry.repository.write.StampRepositoryWrite">

    <select id="create" parameterType="ru.mcko.registry.entity.Stamp" resultType="java.lang.Integer">
        insert into stamps (stamp_link, organization_id, activity_started_at, activity_finished_at, created_at)
        values (#{link}, #{organizationId}, #{activityStartedAt}, #{activityFinishedAt},NOW()) RETURNING id;
    </select>

    <update id="update" parameterType="ru.mcko.registry.entity.Stamp">
        update stamps
        set
            stamp_link = #{link},
            activity_started_at = #{activityStartedAt},
            <if test="activityFinishedAt != null">
                activity_finished_at = #{activityFinishedAt},
            </if>
            updated_at = NOW()
        where 1 = 1
          and id = #{id};
    </update>

    <delete id="delete">
        update stamps
        set
            deleted_at = NOW()
        WHERE 1 = 1
          AND id = #{stampId}
    </delete>

    <delete id="deleteAllByOrganization">
        update stamps
        set
            deleted_at = NOW()
        WHERE 1 = 1
          AND organization_id = #{organizationId}
    </delete>

</mapper>